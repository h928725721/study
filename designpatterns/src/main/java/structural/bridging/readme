将抽象部分与它的实现部分分离，使他们可以独立的变化

比如有图形和颜色两个属性，图形有正方形、长方形、圆形，颜色有红色、蓝色、黄色；按以前的思路：
1.建立形状的抽象类，并实现每个形状的具体抽象类
2.给每个具体抽象类实现颜色

这样就会有3*3个具体实现类属性，类很多

桥接模式，建立图形和颜色的抽象类，各自有各自的具体实现，然后针对不同的属性进行组合，例如，黄色-正方形、蓝色-圆形
可以减少复杂度，并且每个都很方便进行拓展